{
  "name": "@autocrew/widget",
  "version": "1.0.0",
  "private": true,
  "description": "Embeddable chat widget for AutoCrew SaaS",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0"
  }
}
