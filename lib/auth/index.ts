/**
 * Authentication helpers barrel export
 *
 * Server-side session helper functions for authentication and authorization.
 * These work with Better Auth and provide utilities for session management.
 *
 * Usage: import { requireAuth, isSuperAdmin } from '@/lib/auth'
 */

export * from './session-helpers';
