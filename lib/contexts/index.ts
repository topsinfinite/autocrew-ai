/**
 * React contexts barrel export
 *
 * Global state management contexts for client selection and theme management.
 *
 * Usage: import { ClientProvider, ThemeProvider } from '@/lib/contexts'
 */

export * from './client-context';
export * from './theme-context';
